data1是原始数据收集同学收集到的数据样本。
data2是make出来的数据的合集。

每种情绪的最大最小值区间在Save_Excel.xls中每种情绪的最下面两行进行了黄色标注，并保存在angry.txt,depressed1.txt,depressed2.txt,happy1.txt,happy2.txt中。
根据每种情绪期间生成的数据集中保存在了data2.txt中。

情绪预测主程序运行时将data1和data2都导入，合并之后作为了整个数据集。
